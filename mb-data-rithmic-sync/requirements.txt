fastapi==0.115.5
uvicorn[standard]==0.32.1
async_rithmic==1.6.1
supabase==2.10.0
cryptography==44.0.0
PyJWT==2.10.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
apscheduler==3.10.4
# httpx is pulled in transitively by supabase (which pins it >=0.26,<0.28)
# Don't pin it here — let pip resolve to avoid version conflicts.
