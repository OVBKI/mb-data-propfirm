# Supabase — optionnel pour démarrer.
# Sans ces variables, l'application fonctionne en MODE DÉMO avec des données d'exemple.
# Renseigne-les pour passer en mode production (données réelles, persistées).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
